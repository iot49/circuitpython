# IoTPython

IoTPython is a fork of [CircuitPhython](https://github.com/adafruit/circuitpython/) with a added features:

* Module `msgpack`
* Module `iot`
  * `Chronometer`
  * `Ticker`
  * `Timer` (`nrf` port only)
